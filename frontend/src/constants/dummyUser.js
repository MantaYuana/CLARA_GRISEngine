/**
 * Dummy Google user data.
 * Replace this with the actual Google OAuth user object when auth is ready.
 *
 * Expected shape from Google OAuth:
 *  - name        : string
 *  - email       : string
 *  - photoURL    : string | null  (profile picture URL)
 */
export const DUMMY_USER = {
  name: "Dewa Surya",
  email: "dewasurya@gmail.com",
  photoURL: null, // set to a URL string once Google OAuth is wired
};
